# rainwave-write-on-song-change

Writes the current song playing on Rainwave to `current.txt`.

## Setup

1. Install NodeJS
2. Copy `.env.sample` to `.env`
3. Edit `.env` with your Rainwave User ID and key: https://rainwave.cc/keys/
4. `npm ci`
5. `npm run main`
